__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
# Anchored to the repo root on purpose: an unanchored `data/` also matches the
# `eventfm/data/` source package and would silently exclude it from the repo.
/data/
/outputs/
wandb/
*.egg-info/
*.npy
*.npz
